import { Alert } from '@myob/myob-widgets';
import React from 'react';

const SubmittingStatusMessage = () => {
  return (
    <Alert type="info">
      This information is currently being submitted to Inland Revenue.
    </Alert>
  );
};

export default SubmittingStatusMessage;
