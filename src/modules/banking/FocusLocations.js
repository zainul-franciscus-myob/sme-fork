const FocusLocations = {
  SPLIT_ALLOCATION_ACCOUNT_COMBOBOX: 'Split allocation account combobox',

  MATCHED_OR_ALLOCATED_ELEMENT:
    'Matches to an element in the matched or allocate column',
};

export default FocusLocations;
