import { BaseTemplate, PageHead } from '@myob/myob-widgets';
import React from 'react';

const RemittanceAdviceListView = () => {
  return (
    <BaseTemplate>
      <PageHead title="Remittance advice" />
    </BaseTemplate>
  );
};

export default RemittanceAdviceListView;
