#! /bin/sh -eu

# Get the build artefact
export BUCKET=s3://myob-sme-web-ui-bucket

buildkite-agent artifact download build.tar.gz .
tar -zxvf build.tar.gz

# Deploy it
aws s3 cp build/ ${BUCKET} --recursive \
   --cache-control max-age=31104000
